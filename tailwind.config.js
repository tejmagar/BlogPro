/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        './*.php', // Adjust the path according to your folder structure
        './**/*.php', // Include subdirectories if necessary
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}